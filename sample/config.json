{
        "template_dir"  : "templates"
      , "content_dir"   : "data"
      , "output_dir"    : "output"

			, "publish"       : {
					"s3": {
					  "key"       : "api-key"
					, "secret"    : "secret-key"
					, "bucket"    : "bucket-name"
					},

					"sftp": {
					  "host" 	: "hostname"
					, "username"	: "username"
					, "port"        : "22"
					, "private_key" : "~/.ssh/id_rsa"
					, "upload_path" : "remote_path/to/upload"
					}
			}
}
